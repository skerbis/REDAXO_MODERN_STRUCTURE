# VSCode Extension Compatibility Configuration
# Copy this file to .env when using with redaxo-multi-instances-vscode extension

# Instance Configuration
INSTANCE_NAME=modern-structure
REDAXO_SERVERNAME=modern-structure

# REDAXO Docker Image Configuration
# Use 5-stable for PHP 7.4/8.1 or 5-edge for latest PHP/REDAXO versions
REDAXO_IMAGE_TAG=5-stable

# Port Configuration (must be unique across instances)
REDAXO_PORT=8080
HTTPS_PORT=8443
DATABASE_PORT=3306
PHPMYADMIN_PORT=8081

# Database Configuration
DB_NAME=redaxo
DB_USER=redaxo
DB_PASSWORD=redaxo
DB_ROOT_PASSWORD=redaxo
DB_CHARSET=utf8mb4
DB_COLLATION=utf8mb4_unicode_ci

# REDAXO Configuration
REDAXO_LANG=de_de
REDAXO_TIMEZONE=Europe/Berlin
REDAXO_ERROR_EMAIL=admin@modern-structure.local
REDAXO_ADMIN_USER=admin
REDAXO_ADMIN_PASSWORD=redaxo

# SSL/HTTPS Configuration
SSL_ENABLED=false
# When SSL is enabled, REDAXO_SERVER will be set to https://INSTANCE_NAME.local:HTTPS_PORT
# Make sure to add the domain to your hosts file: 127.0.0.1 INSTANCE_NAME.local

# Release Configuration
RELEASE_TYPE=standard

# URLs (automatically configured based on SSL_ENABLED)
# HTTP Mode:  REDAXO_SERVER=http://localhost:REDAXO_PORT
# HTTPS Mode: REDAXO_SERVER=https://INSTANCE_NAME.local:HTTPS_PORT

# MariaDB Version (compatible with VSCode extension)
DB_VERSION=10.11

# Advanced Configuration
# DOWNLOAD_URL=  # Optional: Custom REDAXO download URL